import 'package:flutter/material.dart';

class CreateFoodPlan extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(

      ),
    );
  }
}
