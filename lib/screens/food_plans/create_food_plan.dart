import 'package:flutter/material.dart';

class CreateFoodPlan extends StatelessWidget {
  const CreateFoodPlan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(

      ),
    );
  }
}
