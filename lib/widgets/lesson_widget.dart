import 'package:flutter/material.dart';

import '../models/classes.dart';

class ClassesWidget extends StatelessWidget {
  final Classes classes;

  ClassesWidget({required this.classes});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
